export interface ITheme {
  colors: { primary: string; white: string; text: string }
}

const theme: ITheme = {
  colors: {
    primary: '#2693ff',
    text: '#000',
    white: '#fff',
  },
}

export default theme
