export interface ITheme {
  colors: { primary: string; white: string; text: string }
}

const theme: ITheme = {
  colors: {
    primary: '#ffc926',
    text: '#000',
    white: '#fff',
  },
}

export default theme
